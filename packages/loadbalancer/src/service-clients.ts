import * as cloudApi from '.';

export const { NetworkLoadBalancerServiceClient } = cloudApi.loadbalancer.network_load_balancer_service;
export const { TargetGroupServiceClient } = cloudApi.loadbalancer.target_group_service;
