import * as cloudApi from '.';

export const { BillingAccountServiceClient } = cloudApi.billing.billing_account_service;
export const { BudgetServiceClient } = cloudApi.billing.budget_service;
export const { CustomerServiceClient } = cloudApi.billing.customer_service;
export const { ServiceServiceClient } = cloudApi.billing.service_service;
export const { SkuServiceClient } = cloudApi.billing.sku_service;
