import * as cloudApi from '.';

export const { AddressServiceClient } = cloudApi.vpc.address_service;
export const { GatewayServiceClient } = cloudApi.vpc.gateway_service;
export const { NetworkServiceClient } = cloudApi.vpc.network_service;
export const { RouteTableServiceClient } = cloudApi.vpc.route_table_service;
export const { SecurityGroupServiceClient } = cloudApi.vpc.security_group_service;
export const { SubnetServiceClient } = cloudApi.vpc.subnet_service;
