import * as cloudApi from '.';

export const { DiskPlacementGroupServiceClient } = cloudApi.compute.disk_placement_group_service;
export const { DiskServiceClient } = cloudApi.compute.disk_service;
export const { DiskTypeServiceClient } = cloudApi.compute.disk_type_service;
export const { FilesystemServiceClient } = cloudApi.compute.filesystem_service;
export const { GpuClusterServiceClient } = cloudApi.compute.gpu_cluster_service;
export const { HostGroupServiceClient } = cloudApi.compute.host_group_service;
export const { HostTypeServiceClient } = cloudApi.compute.host_type_service;
export const ComputeImageServiceClient = cloudApi.compute.image_service.ImageServiceClient;
export const { InstanceServiceClient } = cloudApi.compute.instance_service;
export const { PlacementGroupServiceClient } = cloudApi.compute.placement_group_service;
export const { SnapshotScheduleServiceClient } = cloudApi.compute.snapshot_schedule_service;
export const { SnapshotServiceClient } = cloudApi.compute.snapshot_service;
export const { ZoneServiceClient } = cloudApi.compute.zone_service;
