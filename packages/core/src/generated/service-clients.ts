import * as cloudApi from '.';

export const { ApiEndpointServiceClient } = cloudApi.endpoint.api_endpoint_service;
export const { ApiKeyServiceClient } = cloudApi.iam.api_key_service;
export const { IamTokenServiceClient } = cloudApi.iam.iam_token_service;
export const { KeyServiceClient } = cloudApi.iam.key_service;
export const { RoleServiceClient } = cloudApi.iam.role_service;
export const { ServiceAccountServiceClient } = cloudApi.iam.service_account_service;
export const { UserAccountServiceClient } = cloudApi.iam.user_account_service;
export const { YandexPassportUserAccountServiceClient } = cloudApi.iam.yandex_passport_user_account_service;
export const { OperationServiceClient } = cloudApi.operation.operation_service;
