import * as cloudApi from '.';

export const { ExportServiceClient } = cloudApi.logging.export_service;
export const { LogGroupServiceClient } = cloudApi.logging.log_group_service;
export const { LogIngestionServiceClient } = cloudApi.logging.log_ingestion_service;
export const { LogReadingServiceClient } = cloudApi.logging.log_reading_service;
export const { SinkServiceClient } = cloudApi.logging.sink_service;
