import * as cloudApi from '.';

export const { SymmetricCryptoServiceClient } = cloudApi.kms.symmetric_crypto_service;
export const { SymmetricKeyServiceClient } = cloudApi.kms.symmetric_key_service;
