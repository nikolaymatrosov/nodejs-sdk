import * as cloudApi from '.';

export const { GroupMappingServiceClient } = cloudApi.organizationmanager.group_mapping_service;
export const { GroupServiceClient } = cloudApi.organizationmanager.group_service;
export const { OrganizationServiceClient } = cloudApi.organizationmanager.organization_service;
export const { SshCertificateServiceClient } = cloudApi.organizationmanager.ssh_certificate_service;
export const { UserServiceClient } = cloudApi.organizationmanager.user_service;
