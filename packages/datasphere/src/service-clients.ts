import * as cloudApi from '.';

export const { AppTokenServiceClient } = cloudApi.datasphere.app_token_service;
export const { FolderBudgetServiceClient } = cloudApi.datasphere.folder_budget_service;
export const { NodeServiceClient } = cloudApi.datasphere.node_service;
export const { ProjectDataServiceClient } = cloudApi.datasphere.project_data_service;
export const { ProjectServiceClient } = cloudApi.datasphere.project_service;
export const { CommunityServiceClient } = cloudApi.datasphere.community_service;
export const ProjectServiceClientV2 = cloudApi.datasphere.project_service_v2.ProjectServiceClient;
