import * as cloudApi from '.';

export const { CertificateContentServiceClient } = cloudApi.certificatemanager.certificate_content_service;
export const { CertificateServiceClient } = cloudApi.certificatemanager.certificate_service;
